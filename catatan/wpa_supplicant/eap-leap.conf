ctrl_interface=/run/wpa_supplicant
ctrl_interface_group=netdev
update_config=1
country=ID

network={
    ssid="SSID"
    # Kalo kabel pake IEEE8021X
    key_mgmt=WPA-EAP
    eap=LEAP
    identity="USERNAME"
    password="PASSWORD"
}
